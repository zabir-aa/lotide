/*
const _ = require('../index');
const assert = require('chai').assert;

  // TEST CASES

describe('#assertEqual', () => {
  it('returns false for  "Lighthouse Labs", "Bootcamp"', () => {
    assert.strictEqual(_.assertEqual("Lighthouse Labs", "Bootcamp"), false);
  });
  it('returns true for  1, 1', () => {
    assert.strictEqual(_.assertEqual(1, 1), true);
  });
  it('returns true for  "Lighthouse Labs", "Lighthouse Labs"', () => {
    assert.strictEqual(_.assertEqual("Lighthouse Labs", "Lighthouse Labs"), true);
  });
  it('returns false for  1, 3', () => {
    assert.strictEqual(_.assertEqual(1, 3), false);
  });
});
*/